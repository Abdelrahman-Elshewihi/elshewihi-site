# دليل رفع الموقع خطوة بخطوة (Elshewihi.com)

الموقع جاهز بالكامل. باقي بس ترفعه أونلاين وتربطه بالدومين. اتبع الخطوات دي بالترتيب.

## 1) اعمل حساب GitHub (لو مفيش عندك)
- روح على github.com واعمل حساب مجاني.
- اعمل "New repository" باسم مثلاً `elshewihi-site`.
- ارفع كل الملفات والمجلدات اللي جواك (index.html, style.css, script.js, content/, admin/) لل repository ده. أسهل طريقة: من صفحة الـ repo دوس "Add file" → "Upload files"، واسحب الملفات كلها.

## 2) اربط الموقع بـ Netlify
- روح على netlify.com واعمل حساب (تقدر تسجل بحساب GitHub بتاعك مباشرة).
- دوس "Add new site" → "Import an existing project" → اختار GitHub → اختار الـ repo اللي عملته.
- في خانة "Build command" سيبها فاضية، وفي "Publish directory" اكتب `.` (نقطة بس).
- دوس Deploy. بعد دقيقة هيديك لينك مؤقت زي `random-name-123.netlify.app`.

## 3) فعّل تسجيل الدخول (Netlify Identity)
- من داخل الموقع في Netlify، روح "Site configuration" → "Identity" → دوس "Enable Identity".
- في "Registration preferences" اختار "Invite only" (عشان محدش يعمل حساب غيرك إنت).

## 4) فعّل Git Gateway (عشان لوحة التحكم تقدر تحفظ التعديلات)
- لسه في نفس صفحة الـ Identity، روح لقسم "Services" → دوس "Enable Git Gateway".

## 5) ادعُ نفسك كأدمن
- في صفحة الـ Identity، روح تبويب "Identity" فوق → دوس "Invite users" → اكتب إيميلك.
- هيوصلك إيميل، افتحه ودوس "Accept the invite"، وحط باسورد لحسابك.

## 6) جرّب لوحة التحكم
- روح على `الرابط-بتاعك.netlify.app/admin`
- سجل دخول بالإيميل والباسورد.
- هتلاقي "الأعمال" → دوس عليها → أضف أول عمل (صورة + عنوان + وصف) → Publish.
- ارجع لموقعك الأساسي واعمل Refresh، هتلاقي العمل ظهر تلقائي.

## 7) اربط دومين elshewihi.com
- في Netlify: "Domain management" → "Add a domain" → اكتب `elshewihi.com`.
- Netlify هيديك تعليمات لتغيير إعدادات الـ DNS عند الشركة اللي شريت منها الدومين (تحويل الدومين لـ Netlify).
- الـ HTTPS بيتفعل تلقائي ومجاني بعد ما الدومين يترابط (ممكن ياخد لحد ساعة).

## ملاحظات
- كل تعديل تعمله من `/admin` بيتسجل تلقائي على GitHub وبيتحدث على الموقع خلال ثواني.
- تقدر تدخل لوحة التحكم من موبايلك بنفس الطريقة.
- لو حبيت تضيف صفحات تانية بعدين (About, Services, CV...) قولّي وهنوسع الموقع بنفس الأسلوب.
